//arrow 2 param
const add = (num1, num2)=> num1+num2;
//arrow 3 param
const mul = (num1, num2, num3)=> num1*num2*num3;

//arrow single param
const fiveTimes = num => num*5;
//arrow no param
const getName = () => "Brandon";
//arrow complex function
const doMath = (x,y)=>{
    const result =  (x*x) + (2*x*y) + (y*y)
    return result;
}
